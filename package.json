{
  "name": "fll-virtual-robot-tests",
  "version": "0.0.0",
  "private": true,
  "description": "Test-only dependencies for the FLL Virtual Robot simulator. Production code has no build step and pulls deps from CDN; this package.json exists solely for Node-side test harnesses.",
  "scripts": {
    "test:js": "node --test \"tests/js/**/*.test.js\"",
    "test:py": "node tests/py/run-mpy.js",
    "test:py:cpython": "python3 tests/py/run.py",
    "test": "npm run test:py && npm run test:js"
  },
  "devDependencies": {
    "@micropython/micropython-webassembly-pyscript": "1.28.0-6"
  }
}
